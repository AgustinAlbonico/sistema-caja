# Configuracion del launcher para entorno LAN (IPv4)
# Copiar este archivo a launcher.env y ajustar valores por PC.

# Puerto local donde el backend debe quedar escuchando.
# El frontend del proyecto usa por defecto 47832 en llamadas API.
BACKEND_PORT=47832

# Puerto local del frontend (Vite).
FRONTEND_PORT=5173

# URL que abre el launcher al finalizar.
APP_URL=http://127.0.0.1:5173/login

# Timeout maximo de espera por servicio (en segundos).
START_TIMEOUT_SECONDS=60
